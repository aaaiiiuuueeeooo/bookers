<h1>Editing Book</h1>

<%= form_with model: @book, url: update_list_path(@book.id), method: :patch do |f| %>

    <label>Title</label>
    <%= f.text_field :本のタイトル %>
    
    <label>Body</label>
    <%= f.text_area :感想 %>
    
    <%= f.submit 'Update Book' %>

<% end %>    