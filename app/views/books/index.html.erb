<html>
<head></head>
<body>
  <p id="notice"></p>
  <h1>Books</h1>
  <table>
    <thead>
      <tr>
        <p>Title</p>
        <span><%= @book.本のタイトル %></span>
        <p>Body</p>
        <span><%= @book.感想 %></span>
        <% @books.each do |list| %>
        <th >
          <a href='books/show'>show</a>
          <a href='books/edit'>edit</a>
          <a href='lists/destroy'>destroy</a>
        </th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      
    </tbody>
  </table>
  <h2>New book</h2>
  <%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>

</body>
</html>